# CoreGeek 《未来战争》参赛 AI — 项目设计文档

> 版本 v1.0 | 2026-09-20 | 对应任务书 v1.0 & 接口文档 v1.0

---

## 1 项目背景

本项目是云核心网第十届编程大赛《未来战争》的参赛选手程序。游戏为双人对战 TD（塔防）策略游戏，两位参赛者各控制一支人类队伍，在白天采集资源、建造防御工事、完成任务，在夜晚操控武器抵御机器人进攻浪潮，存活 10 天（1300 回合）或摧毁对方基地即可获胜。

判题系统通过 HTTP 请求向选手程序发送当前地图状态（Request），选手程序需在 5 秒内返回各角色调度指令（Response）。本项目实现了完整的 HTTP 选手服务，包含 Request 解析、回合决策（完整智能策略）、Response 序列化，并集成了 LLM prompt 构造与沙盒命令执行框架。

### 1.1 技术栈

| 项目 | 说明 |
|------|------|
| 语言 | Java 8+ |
| HTTP 框架 | `com.sun.net.httpserver.HttpServer`（JDK 内置） |
| JSON 序列化 | Gson 2.8.2（`lib/gson-2.8.2.jar`） |
| 构建方式 | `javac` 命令行编译，`makelist.txt` 管理源文件列表 |
| 运行方式 | `java -cp "bin;lib/*" com.huawei.Main <port>` |

### 1.2 目录结构

```
D:\Codes\
├── doc/                        # 任务书、接口文档、样例数据、本文档
│   ├── 任务书v1.0.md            # 游戏规则全文
│   ├── 接口文档v1.0.md          # HTTP Request/Response 字段定义
│   ├── request.txt             # Request JSON 样例
│   ├── response.txt            # Response JSON 样例
│   └── 项目设计文档.md          # 本文档
├── CoreGeek/                   # 选手代码（本项目的全部实现）
│   ├── lib/gson-2.8.2.jar
│   ├── src/main/java/com/huawei/
│   │   ├── Main.java           # 入口，启动 HttpServer
│   │   ├── MyHttpHandler.java  # HTTP 请求处理器
│   │   ├── model/              # 数据模型（POJO）
│   │   ├── strategy/           # 决策引擎
│   │   ├── llm/                # LLM 客户端
│   │   ├── sandbox/            # 沙盒命令执行器
│   │   └── util/               # 工具类
│   └── src/main/resources/log4j.properties
└── makelist.txt                # 编译用源文件列表（相对 src/ 目录）
```

---

## 2 游戏核心规则摘要

### 2.1 地图与时间

- 地图为 41×32 矩形，坐标原点 (0,0) 在左下角，水平向右为 X 轴，垂直向上为 Y 轴。
- 一天 = 130 回合：白天 70 回合（采集/建造/任务），夜晚 60 回合（防御机器人）。
- 单场比赛最大 1300 回合（10 天）。
- 双方基地分别位于地图左上（挑战者 challenger）和右下（防守者 defender）区域。

### 2.2 角色

| 角色 | HP | 背包 | 人数 | 能力 |
|------|----|------|------|------|
| 开拓者 pioneer | 200 | 40 格 | 1 | 接任务、提交答案、召唤宝藏 |
| 工人 worker | 220 | 100 格 | 2 | 采集矿石、建造/拆除围墙与武器 |

- 角色阵亡后第二天白天开始后 20 回合在基地复活，背包物品保留。

### 2.3 建筑

| 建筑 | 英文 | 说明 |
|------|------|------|
| 基地 | station | 2×2 大小，pos 传左上角坐标，被摧毁则失败 |
| 加特林炮台 | gatling | 攻击距离 4，可多目标（须在同一 90° 锥内） |
| 电磁狙击炮 | railgun | 攻击距离 7，单目标穿透 |
| 火箭发射台 | rocket | 全图射程，3×3 溅射，3 回合冷却 |
| 围墙 | wall | 需消耗石头建造，可拆除但不回收 |

- 武器工事全局最多 3 座，需角色站在武器周围一格内操控攻击。
- 新建武器等级 level1，可通过升级券升级到 level3，升级后回满血。

### 2.4 机器人

| 机器人 | 攻击力 | 攻击距离 | HP | 积分 |
|--------|--------|----------|----|------|
| 小型 smallRobot | 5 | 3 | 40 | 1 |
| 中型 middleRobot | 10 | 3 | 60 | 2 |
| 大型 largeRobot | 20 | 3 | 500 | 4 |
| BOSS bossRobot | 40 | 3 | 800 | 10 |

- 机器人在夜晚第一回合出现，攻击阻挡其移动的单位。
- 黑夜结束后残余机器人自动清除。

### 2.5 昼夜判定

```
回合号 roundNo 取模 130：
  0 ~ 69  → 白天（可采集/建造/卖矿/买物/接任务/提交/召唤宝藏/移动/使用/丢弃）
  70 ~ 129 → 夜晚（可攻击/移动/使用/丢弃，不可建造/采集）
```

### 2.6 距离计算

所有距离均采用**切比雪夫距离**：`max(|x1-x0|, |y1-y0|)`。角色可朝 8 个方向移动，每次移动一格。

---

## 3 HTTP 接口说明

选手程序以 HTTP Server 方式运行，监听指定端口，IP 为 `0.0.0.0`。判题器每回合发送一个 POST 请求，Body 为 Request JSON，选手需返回 Response JSON。

### 3.1 Request 顶层结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `roundNo` | int | 当前回合数（0~1299） |
| `mapInfo` | MapInfo | 地图整体信息（宽度、高度、中立元素） |
| `teamOur` | TeamOur | 我方队伍全部信息 |
| `teamEnemy` | TeamEnemy | 敌方队伍可见信息（基地和围墙全图可见，其他单位需进入视野） |
| `robot` | Robot | 当前场上所有存活机器人信息（全图可见） |
| `phaseTask` | String | 当前已领取任务的原文描述，空字符串表示无任务 |
| `lastRoundRoleActionResults` | Map\<String,Boolean\> | 上回合各角色动作是否合法 |
| `lastSummonTreasureResult` | int | 上回合召唤宝藏结果码（0=未探测,1=成功,2=无宝藏/未到时间,3=献祭物品错误,4=宝藏已空） |
| `llmResp` | String | 上回合 LLM 返回的响应内容 |
| `worldNews` | WorldNews | 世界新闻（officialNews 官方消息 + folkLegends 民间传闻） |
| `lastCmdResult` | String | 上回合 executeCmd 执行结果，格式 `[exitCode:N]\n<输出>` |
| `vendorShopList` | Array\<ShopItem\> | 小贩收购矿石清单 `{name, price}` |
| `weaponShopList` | Array\<ShopItem\> | 武器商店出售物品清单 `{name, price}` |
| `errors` | Array\<Error\> | 本轮错误信息 `{errorCode, description}` |

### 3.2 MapInfo — 地图信息

| 字段 | 类型 | 说明 |
|------|------|------|
| `width` | int | 地图 X 轴总宽度（41） |
| `height` | int | 地图 Y 轴总高度（32） |
| `zones` | Zone[] | 中立元素列表 |

**Zone.neutralType 取值：**

| 值 | 说明 |
|----|------|
| `stone` / `iron` / `copper` | 石矿 / 铁矿 / 铜矿 |
| `vendor` | 小贩（卖矿换金币） |
| `weaponShop` | 武器商店（买物品） |
| `challengerTaskPoint1` / `challengerTaskPoint2` | 挑战者任务点 1/2 |
| `defenderTaskPoint1` / `defenderTaskPoint2` | 防守者任务点 1/2 |

### 3.3 Role — 单位属性

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 全局唯一编号 |
| `pos` | Pos `{x, y}` | 坐标 |
| `roleType` | String | 单位分类（见下表） |
| `health` | int | 当前血量 |
| `attackPower` | int | 攻击力 |
| `attackRange` | int | 攻击距离 |
| `backPackCapability` | int | 背包容量上限 |
| `backpack` | String[] | 背包内物品名称列表 |
| `level` | int | 等级（仅建筑持有，1~3） |
| `cooldown` | int | 武器冷却剩余回合数（仅火箭有） |

**roleType 取值：** `station` / `gatling` / `railgun` / `rocket` / `wall` / `pioneer` / `worker`

**角色 ID 分配规则：**

| 阵营 | Challenger | Defender | 角色 |
|------|-----------|----------|------|
| worker1 | 10010 | 20010 | 工人 |
| pioneer | 10011 | 20011 | 开拓者 |
| worker2 | 10012 | 20012 | 工人 |
| station | 10013 | 20013 | 基地 |
| gatling | 10020/10021/10022 | 20020/20021/20022 | 加特林炮台 |
| railgun | 10030/10031/10032 | 20030/20031/20032 | 电磁狙击炮 |
| rocket | 10040/10041/10042 | 20040/20041/20042 | 火箭发射台 |
| wall | 40000/40001/... | 41000/41001/... | 围墙 |

### 3.4 PlayerTask — 任务点信息

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskType` | String | `自进化类1` / `自进化类2` |
| `taskPosition` | Pos | 任务点坐标 |
| `coldDownRounds` | int | 冷却剩余回合数，0=可接取 |
| `scoreReward` | int | 积分奖励 |
| `goldReward` | int | 金币奖励 |
| `isValid` | bool | 是否允许领取 |
| `timeoutRounds` | int | 任务超时回合数 |

### 3.5 RobotRole — 机器人单位

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 机器人唯一编号 |
| `pos` | Pos | 当前坐标 |
| `roleType` | String | `smallRobot` / `middleRobot` / `largeRobot` / `bossRobot` |
| `health` | int | 当前血量 |
| `abnormalState` | String | `"dizzy"`=被眩晕，`""`=正常 |
| `targetTeam` | String | 攻击目标阵营：`"challenger"` / `"defender"` |

### 3.6 Response 顶层结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `roleCommandMap` | Map\<int, RoleCommand\> | 全角色指令集合，key=角色ID，value=动作指令 |
| `prompt` | String | 提交给 LLM 的 prompt（每个游戏日限 3 次，任务期间不限） |
| `executeCmd` | String | 沙盒环境中执行的命令（仅任务期间可用，15 秒超时） |

### 3.7 RoleCommand — 单角色指令

| 字段 | 类型 | 必选 | 说明 |
|------|------|:----:|------|
| `action` | String | 是 | 动作标识（见动作码表） |
| `controllerId` | String | 否 | 操控武器攻击的角色 ID |
| `targetPos` | Array\<Pos\> | 否 | 目标坐标数组 |
| `name` | String | 否 | 操作对象名称（物品/建筑名） |
| `num` | int | 否 | 购买/贩卖数量，默认 1 |
| `taskAnswer` | String | 否 | 任务作答内容 |
| `item` | Array\<String\> | 否 | 召唤宝藏献祭物品名称数组 |

**动作码全集：**

| 动作码 | 说明 | 关键参数 |
|--------|------|----------|
| `move` | 移动一格 | `targetPos`（1 个目标位置） |
| `attack` | 操控武器攻击（仅夜晚） | `controllerId` + `targetPos`（加特林/火箭可多目标） |
| `sell` | 卖矿石换金币 | `name` + `num` |
| `buy` | 购买商品 | `name` + `num` |
| `build` | 建造（仅工人白天） | `name`（建筑名）+ `targetPos` |
| `remove` | 拆除围墙（仅工人） | `targetPos` |
| `acceptTask` | 领取任务（仅开拓者） | 无 |
| `submitAnswer` | 提交任务答案（仅开拓者） | `taskAnswer` |
| `summonTreasure` | 召唤宝藏（仅开拓者） | `targetPos` + `item`（献祭物品） |
| `use` | 使用背包物品 | `name`，部分物品需 `targetPos` |
| `drop` | 丢弃背包物品 | `name` |
| `collect` | 采集矿石（仅工人） | `targetPos`（矿石坐标） |

> 每个角色每回合至多提交 1 条指令。

### 3.8 错误码

| errorCode | 说明 |
|-----------|------|
| 0 | 未知错误 |
| 1 | 任务超时 |
| 2 | 答案错误 |
| 3 | 网络错误 |
| 4 | 指令错误 |
| 5 | LLM 额度超限（每个游戏日 3 次，任务期间不限） |

### 3.9 异常处理机制

- 请求响应超时：连接超 10 秒、响应超 5 秒判超时。
- 响应格式错误：返回数据不符合接口规范。
- 指令错误：指令字段缺失或无法识别。
- 单队异常累计 5 次后不再调度，异常退出后不重启。

---

## 4 项目架构

### 4.1 分包结构

```
com.huawei
├── Main                    # 程序入口，启动 HttpServer
├── MyHttpHandler           # HTTP 请求处理器（解析 Request → 调用引擎 → 返回 Response）
│
├── model/                  # 数据模型层（POJO）
│   ├── GameRequest         # 顶层 Request
│   ├── GameResponse        # 顶层 Response
│   ├── RoleCommand         # 单角色指令（含 12 种动作工厂方法）
│   ├── GameContext         # 跨回合状态缓存
│   ├── Pos                 # 坐标（含切比雪夫距离计算）
│   ├── Zone                # 中立区域元素
│   ├── MapInfo             # 地图信息
│   ├── Role                # 单位通用属性
│   ├── PlayerTask          # 任务点信息
│   ├── TeamOur             # 我方队伍信息
│   ├── TeamEnemy           # 敌方队伍信息
│   ├── Robot               # 机器人集合
│   ├── RobotRole           # 单个机器人
│   ├── WorldNews           # 世界新闻
│   ├── ErrorInfo           # 错误信息
│   └── ShopItem            # 商店物品条目
│
├── strategy/               # 决策引擎层
│   ├── DecisionEngine      # 总调度入口（判定昼夜，分发策略）
│   ├── DayStrategy         # 白天策略调度
│   ├── NightStrategy       # 夜晚策略调度
│   ├── WorkerStrategy      # 工人行为逻辑
│   ├── PioneerStrategy     # 开拓者行为逻辑
│   ├── CombatStrategy      # 武器操控与目标选择
│   └── TaskSolver          # 自进化任务解题状态机
│
├── llm/
│   └── LlmClient           # LLM prompt 构造与响应解析
│
├── sandbox/
│   └── SandboxExecutor     # 沙盒命令构造与结果解析
│
└── util/
    ├── Constants           # 常量定义（角色类型/动作码/物品名/时间参数）
    ├── MapUtil             # 地图工具（距离/寻路/碰撞/昼夜判定）
    └── RoleUtil            # 角色工具（查找/背包/存活判定）
```

### 4.2 请求处理流程

```
判题器 POST Request JSON
        │
        ▼
MyHttpHandler.handle()
        │  Gson 反序列化 → GameRequest
        ▼
DecisionEngine.decide(request)
        │
        ├── initContext()        # 首回合初始化阵营/基地位置
        ├── ctx.refreshDay()     # 检测换日重置 LLM 计数
        ├── ctx.recordMines()    # 记录已知矿点
        ├── countWeapons()       # 统计武器工事数量
        │
        ├── isDayTime?
        │   ├── true  → DayStrategy.execute()
        │   │            ├── WorkerStrategy   # 工人采集→卖矿→建造→升级
        │   │            └── PioneerStrategy   # 开拓者接任务→解题→提交
        │   │
        │   └── false → NightStrategy.execute()
        │                ├── CombatStrategy    # 武器攻击（优先分配操控者）
        │                ├── WorkerStrategy    # 工人移动到武器旁
        │                └── PioneerStrategy   # 开拓者移动到武器旁
        │
        ▼
GameResponse {roleCommandMap, prompt, executeCmd}
        │
        ▼
Gson 序列化 → Response JSON → 返回判题器
```

### 4.3 跨回合状态管理

`GameContext` 实例由 `DecisionEngine` 持有，在服务生命周期内跨回合保留：

| 字段 | 类型 | 说明 |
|------|------|------|
| `teamType` | String | 阵营标识，首次请求时初始化 |
| `stationPos` | Pos | 基地坐标，每回合更新 |
| `llmCallCountToday` | int | 本游戏日 LLM 调用次数，换日重置 |
| `currentDay` | int | 当前游戏日索引 |
| `treasureOpened` | bool | 宝藏是否已开启 |
| `weaponCount` | int | 已建造武器工事数量 |
| `taskState` | TaskState | 自进化任务状态机枚举 |
| `currentTaskPointIdx` | int | 当前任务点索引 |
| `taskAcceptRound` | int | 领取任务时的回合数 |
| `taskTimeoutRounds` | int | 当前任务超时回合数 |
| `bestAnswer` | String | 任务当前最佳答案 |
| `pendingCmds` | LinkedList\<String\> | 沙盒待执行命令队列 |
| `awaitingCmdResult` | bool | 是否正在等待沙盒结果 |
| `knownMines` | Map\<String,Set\<Pos\>\> | 已知矿点（按类型分组） |
| `workerTargetMine` | Map\<int,Pos\> | 工人目标矿点分配 |
| `workerMining` | Map\<int,Boolean\> | 工人状态（采集/运输） |

---

## 5 各模块详细设计

### 5.1 Main — 程序入口

启动 `HttpServer`，绑定命令行传入的端口号，将 `/` 路径映射到 `MyHttpHandler`，使用固定线程池（10 线程）处理并发请求。

### 5.2 MyHttpHandler — HTTP 处理器

1. 读取 POST Body 为 UTF-8 字符串。
2. 用 Gson 反序列化为 `GameRequest`。
3. 调用 `DecisionEngine.decide()` 生成 `GameResponse`。
4. 用 Gson 序列化为 JSON 返回，Content-Type 设为 `application/json;charset=utf-8`。
5. 异常时返回 `{}` 避免 5 秒超时。

### 5.3 DecisionEngine — 决策总调度

- 持有 `GameContext` 跨回合状态。
- 每回合：初始化/更新上下文 → 判定昼夜 → 分发到 `DayStrategy` 或 `NightStrategy`。
- `occupiedRoles` 集合确保同一角色不被多个策略重复分配指令。

### 5.4 DayStrategy — 白天策略

调度工人和开拓者执行白天行为：

**工人（WorkerStrategy）：**
1. 背包快满（剩余 ≤5）→ 移动到小贩 → `sell` 卖最值钱的矿石。
2. 武器工事 <3 且有石头 → 在基地附近 `build` 围墙。
3. 背包有升级券且金币足够 → 移动到建筑旁 `use` 升级；否则移动到武器商店 `buy` 升级券。
4. 正常采集：分配最近矿点 → 移动到矿旁 → `collect`。
5. 无事可做 → 移动到武器商店待命。

**开拓者（PioneerStrategy）：**
1. 血量 <30% 且有药品 → `use Medicine`。
2. 调用 `TaskSolver` 处理自进化任务。
3. 无任务时 → 尝试召唤宝藏（背包有任务用品且未开启）。
4. 否则 → 移动到任务点待命。

### 5.5 NightStrategy — 夜晚策略

1. **CombatStrategy** 优先执行：为每座武器分配操控角色，选择攻击目标。
2. 未分配指令的工人和开拓者 → 移动到最近武器旁待命。
3. 血量 <30% 且有药品 → `use Medicine`。

### 5.6 CombatStrategy — 武器操控与目标选择

**目标筛选：** 优先攻击 `targetTeam` 为我方的机器人；若无则攻击所有可见机器人。

**威胁值计算：** `威胁 = 机器人基础分 × 当前血量`，BOSS(10) > 大型(4) > 中型(2) > 小型(1)。

**加特林（gatling）：**
- 等级决定目标数：level1=1，level2=2，level3=3。
- 多目标须在同一 90° 锥形内（任意两目标相对加特林的方向夹角 ≤ 90°）。
- 每颗子弹沿弹道飞行，命中该弹道上最近的机器人。

**电磁炮（railgun）：**
- 单目标，选择攻击距离内（7 格）威胁最大的机器人。
- 能量沿弹道穿透，每只机器人受 min(剩余能量, 当前血量) 伤害。

**火箭（rocket）：**
- 冷却 `cooldown > 0` 时跳过。
- 等级决定导弹数。
- 落点选择机器人最密集的 3×3 区域，中心伤害 20，溅射 10。

**操控者选择：** 优先选武器周围一格内的非建筑角色；若无则选最近的角色（策略层会先移动到武器旁）。

### 5.7 TaskSolver — 自进化任务状态机

```
IDLE
  │ 找到可用任务点(isValid && coldDownRounds==0)
  ▼
MOVING_TO_TASK_POINT
  │ 到达任务点相邻格 → acceptTask
  ▼
TASK_ACCEPTED
  │ 构造 LLM prompt（任务期间不限次数）
  ▼
AWAIT_LLM
  │ 收到 llmResp → 提取答案和/或命令
  │   ├── 有命令 → 提交 executeCmd → AWAIT_CMD
  │   └── 仅答案 → READY_TO_SUBMIT
  ▼
AWAIT_CMD
  │ 收到 lastCmdResult → 解析输出作为答案
  ▼
READY_TO_SUBMIT
  │ submitAnswer(bestAnswer)
  ▼
SUBMITTED → IDLE（下回合重置）
```

**错误处理：** 收到 errorCode=1（任务超时）或 errorCode=2（答案错误）时重置状态机为 IDLE。

### 5.8 LlmClient — LLM 客户端

- **canCallLlm()**：任务期间（`phaseTask` 非空）不限次数；非任务期间每游戏日限 3 次。
- **buildTaskPrompt()**：构造自进化任务解题 prompt，附任务描述 + 上次 LLM 返回。
- **buildTreasurePrompt()**：构造宝藏推理 prompt，要求 JSON 格式输出位置/物品/时间。
- **extractAnswer()**：从 LLM 响应中提取答案文本，去除 markdown 代码块标记。
- **extractCommand()**：从 LLM 响应中提取 `bash`/`python` 代码块作为沙盒命令。

### 5.9 SandboxExecutor — 沙盒执行器

- **parseResult()**：解析 `lastCmdResult`，识别三种格式：
  - `[exitCode:N]\n<输出>` — 正常执行
  - `[TIMEOUT]\n<部分输出>` — 命令超时
  - `[JUDGER_ERROR]\n<原因>` — 判题器异常
  - 输出末尾 `[TRUNCATED]` 表示超过 64KB 被截断
- **CmdResult**：封装解析结果（exitCode/timeout/judgerError/truncated/output）。
- **buildPythonCmd() / buildShellCmd()**：构造沙盒命令字符串。

### 5.10 MapUtil — 地图工具

| 方法 | 说明 |
|------|------|
| `chebyshev(a, b)` | 切比雪夫距离 |
| `isAdjacent(a, b)` | 是否相邻（距离=1） |
| `isAdjacentOrSame(a, b)` | 是否相邻或重叠（距离≤1） |
| `stepToward(from, to)` | 8 方向朝目标移动一步 |
| `nextStepToward(from, target, obstacles, w, h)` | 考虑障碍物的下一步移动（直接→水平→垂直→对角） |
| `isValidPos(p, w, h)` | 坐标是否在地图范围内 |
| `isBlocked(p, obstacles)` | 坐标是否被障碍物占据 |
| `isDayTime(roundNo)` | 判定白天（roundNo%130 < 70） |
| `getDay(roundNo)` | 获取游戏日索引（roundNo/130） |
| `findZones(zones, type)` | 按类型查找中立区域 |
| `findNearestRole(roles, pos)` | 找距离最近的角色 |
| `collectObstacles(...)` | 收集所有占用坐标作为障碍物列表 |

### 5.11 RoleUtil — 角色工具

| 方法 | 说明 |
|------|------|
| `filterByType(roles, type)` | 按角色类型筛选 |
| `findFirst(roles, type)` | 查找第一个指定类型角色 |
| `findById(roles, id)` | 按 ID 查找角色 |
| `countInBackpack(role, name)` | 统计背包中指定物品数量 |
| `hasInBackpack(role, name)` | 判断背包是否有物品 |
| `isBackpackFull(role)` | 背包是否已满 |
| `backpackRemaining(role)` | 背包剩余空间 |
| `isAlive(roles, id)` | 角色是否存活（在列表中） |
| `getStationTiles(pos)` | 获取基地 4 格坐标（2×2） |
| `isAdjacentTo(role, pos)` | 角色是否在目标相邻格内 |

### 5.12 Constants — 常量定义

集中定义所有魔法值：
- 阵营：`challenger` / `defender`
- 角色类型：`station` / `gatling` / `railgun` / `rocket` / `wall` / `pioneer` / `worker`
- 机器人类型：`smallRobot` / `middleRobot` / `largeRobot` / `bossRobot`
- 矿石类型：`stone` / `iron` / `copper`
- 中立元素：`vendor` / `weaponShop`
- 动作码：`move` / `attack` / `sell` / `buy` / `build` / `remove` / `acceptTask` / `submitAnswer` / `summonTreasure` / `use` / `drop` / `collect`
- 物品名：升级券、消耗品、召唤令、任务用品
- 时间参数：`ROUNDS_PER_DAY=130` / `DAY_ROUNDS=70` / `NIGHT_ROUNDS=60` / `MAX_ROUNDS=1300`
- 角色属性：`PIONEER_HP=200` / `WORKER_HP=220` / `MAX_WEAPONS=3` 等

---

## 6 商店物品参考

### 6.1 升级券

| 商品 | 英文名 | 效果 | 价格 |
|------|--------|------|------|
| 武器升级券1 | WeaponUpgradeVoucher1 | 武器 level1→2 | 100 |
| 武器升级券2 | WeaponUpgradeVoucher2 | 武器 level2→3 | 150 |
| 围墙升级券1 | WallUpgradeVoucher1 | 围墙 level1→2 | 20 |
| 围墙升级券2 | WallUpgradeVoucher2 | 围墙 level2→3 | 30 |
| 基地升级券1 | StationUpgradeVoucher1 | 基地 level1→2 | 100 |
| 基地升级券2 | StationUpgradeVoucher2 | 基地 level2→3 | 150 |

### 6.2 消耗品

| 商品 | 英文名 | 效果 | 价格 |
|------|--------|------|------|
| 围墙修复包 | WallFixer | 围墙回满血（需相邻） | 10 |
| 生命药剂 | Medicine | 使用者回满血 | 10 |
| 眩晕法宝 | DizzyWeapon | 3×3 机器人眩晕 5 回合 | 100 |
| 范围炸弹 | Bomb | 3×3 机器人 100 伤害 | 100 |
| 小型机器人召唤令 | SmallRobotSummonOrder | 对方下夜小型+1 | 20 |
| 中型机器人召唤令 | MiddleRobotSummonOrder | 对方下夜中型+1 | 30 |
| 大型机器人召唤令 | LargeRobotSummonOrder | 对方下夜大型+1 | 100 |
| BOSS 召唤令 | BossRobotSummonOrder | 对方下夜 BOSS+1 | 200 |

### 6.3 任务用品

| 商品 | 英文名 | 价格 |
|------|--------|------|
| 古符石板 | AcientTablet | 15 |
| 星辰之沙 | StarSand | 15 |
| 烈焰之息 | FlameBreath | 15 |
| 寒霜药剂 | FrostPotion | 15 |
| 荆棘护符 | ThornAmulet | 15 |
| 回音铁哨 | IronWhistle | 15 |

---

## 7 编译与运行

### 7.1 编译

```bash
cd D:/Project/programming_contest/code_5g/CoreGeek/src
javac -Xlint:unchecked -Xlint:-options -source 1.8 -target 1.8 \
  -d ../bin -encoding UTF-8 -Djava.ext.dirs=../lib @../../makelist.txt
```

- `makelist.txt` 位于 `D:/Codes/` 根目录，列出所有源文件路径（相对于 `src/`）。
- 编译输出到 `CoreGeek/bin/`。

### 7.2 运行

```bash
cd D:/Project/programming_contest/code_5g/CoreGeek
java -cp "bin;lib/*" com.huawei.Main 8080
```

- `port` 由判题系统启动时传入。
- JDK 9+ 不支持 `-Djava.ext.dirs`，需使用 `-cp` 方式加载 Gson。

### 7.3 测试

```bash
# 启动服务后，用样例数据测试
curl -s -X POST -H "Content-Type: application/json" \
  -d @D:/Project/programming_contest/doc/request.txt http://localhost:8080/
```

预期返回类似：
```json
{
  "roleCommandMap": {
    "10040": {"action":"attack","controllerId":"10010","targetPos":[{"x":4,"y":4}]},
    "10012": {"action":"move","targetPos":[{"x":9,"y":17}]},
    "10011": {"action":"move","targetPos":[{"x":9,"y":13}]}
  },
  "prompt": "",
  "executeCmd": ""
}
```

### 7.4 打包提交

```bash
cd D:/Project/programming_contest/code_5g
tar --exclude=".idea" --exclude="out" --exclude="bin" \
  --exclude="README.md" --exclude="input.json" --exclude="output.json" \
  --exclude="cache.json" -czvf Java_SDK.tar.gz CoreGeek makelist.txt
```

---

## 8 积分规则

$$\text{总积分} = score_1 + score_2 + score_3$$

| 分项 | 公式 | 说明 |
|------|------|------|
| $score_1$ | 任务积分奖励 + 5 × (标准回合数 / 实际完成回合) | 任务完成时 |
| $score_1$ | 任务积分奖励 × 通过率 | 部分完成时（通过率=正确字段/全量字段） |
| $score_2$ | Σ(机器人击杀数 × 机器人积分) | 小=1, 中=2, 大=4, BOSS=10 |
| $score_3$ | Σ 10×day×存活系数 | 存活系数=1(基地存活)/0(基地被毁当天及之后) |

---

## 9 设计决策与注意事项

### 9.1 指令合法性预检

为避免异常响应累计 5 次导致出局，代码在生成指令时进行预检：
- 夜晚不生成 `build`/`collect` 指令。
- 白天不生成 `attack` 指令。
- 仅工人生成 `build`/`collect`/`remove` 指令。
- 仅开拓者生成 `acceptTask`/`submitAnswer`/`summonTreasure` 指令。
- `move`/`attack`/`build`/`collect` 必须携带 `targetPos`。
- `sell`/`buy`/`use`/`drop` 必须携带 `name`。
- 阵亡角色（不在 `roles` 列表中）不生成指令。

### 9.2 超时规避

- 决策逻辑控制在秒级以内，避免 5 秒响应超时。
- `MyHttpHandler` 异常时返回 `{}` 而非不响应。

### 9.3 LLM 调用策略

- 每游戏日 3 次限额，换日重置（`GameContext.refreshDay()`）。
- 自进化任务期间（`phaseTask` 非空）不限次数且不计入日常限额。
- LLM 响应通过 `llmResp` 字段下回合返回，采用异步等待模式。

### 9.4 沙盒命令执行

- `executeCmd` 仅在任务期间可用，判题器执行后通过 `lastCmdResult` 返回。
- 执行超时 15 秒，沙盒故障与超时不计入异常次数。
- `SandboxExecutor` 解析 `[exitCode:N]` 格式判断成功与否。

### 9.5 移动碰撞处理

- `MapUtil.nextStepToward()` 考虑障碍物，按优先级尝试：直接朝目标 → 水平 → 垂直 → 对角。
- 障碍物列表包含己方/敌方建筑、角色、机器人、矿点、任务点等所有阻挡单位。
- 移动失败时角色停留在原位，不生成无效指令。

---

## 10 完整 API 参考

> 以下列出每个类的全部 public 方法和关键 private 方法的签名与职责，供二次开发时快速定位修改点。

### 10.1 入口与 HTTP 层

#### Main
```java
public static void main(String[] args) throws IOException
```
启动 HttpServer，绑定 `args[0]` 端口，线程池 10，路径 `/` → `MyHttpHandler`。

#### MyHttpHandler (implements HttpHandler)
```java
public void handle(HttpExchange httpExchange)               // 入口：读 Body → Gson 解析 → engine.decide() → 返回 JSON
private String getRequestBody(HttpExchange httpExchange)    // 读取 POST Body 为 UTF-8 字符串
private void sendResponse(HttpExchange httpExchange, String responseBody) // 发送 JSON 响应，Content-Type=application/json;charset=utf-8
```
- 字段：`Gson gson`、`DecisionEngine engine`（实例字段，跨请求复用）。

### 10.2 model 包

#### Pos
```java
public Pos()
public Pos(int x, int y)
public int chebyshevTo(Pos other)          // 切比雪夫距离
public int chebyshevTo(int ox, int oy)
public boolean equals(Object o)
public int hashCode()
public String toString()                   // "(x,y)"
```
- 字段：`int x`, `int y`（public，Gson 直接映射）。

#### RoleCommand
```java
// 构造
public RoleCommand()
public RoleCommand(String action)

// 工厂方法（全部 static，返回填好字段的 RoleCommand）
public static RoleCommand move(Pos target)                           // move + targetPos[1]
public static RoleCommand attack(String controllerId, List<Pos> targets) // attack + controllerId + targetPos[N]
public static RoleCommand collect(Pos target)                        // collect + targetPos[1]
public static RoleCommand build(String name, Pos target)             // build + name + targetPos[1]
public static RoleCommand remove(Pos target)                         // remove + targetPos[1]
public static RoleCommand sell(String name, int num)                 // sell + name + num
public static RoleCommand buy(String name, int num)                  // buy + name + num
public static RoleCommand use(String name)                           // use + name（无坐标）
public static RoleCommand use(String name, Pos target)              // use + name + targetPos[1]（有坐标）
public static RoleCommand drop(String name)                          // drop + name
public static RoleCommand acceptTask()                               // acceptTask
public static RoleCommand submitAnswer(String answer)                // submitAnswer + taskAnswer
public static RoleCommand summonTreasure(Pos target, List<String> items) // summonTreasure + targetPos[1] + item[N]
```
- 字段：`String action`, `String controllerId`, `List<Pos> targetPos`, `String name`, `Integer num`, `String taskAnswer`, `List<String> item`。
- **扩展方式**：如需新增动作码，在 `Constants` 中定义动作常量，在 `RoleCommand` 中添加对应工厂方法。

#### GameContext
```java
public void refreshDay(int roundNo)      // 检测换日，重置 llmCallCountToday
public void recordMines(MapInfo mapInfo) // 将 zones 中的矿点记录到 knownMines
```
- **public 字段**（跨回合状态，策略层直接读写）：

| 字段 | 类型 | 初始值 | 用途 |
|------|------|--------|------|
| `teamType` | String | `""` | 阵营标识，首回合初始化 |
| `stationPos` | Pos | `new Pos()` | 基地左上角坐标，每回合更新 |
| `llmCallCountToday` | int | `0` | 本日 LLM 调用次数 |
| `currentDay` | int | `-1` | 当前游戏日索引 |
| `treasureOpened` | boolean | `false` | 宝藏是否已开启 |
| `weaponCount` | int | `0` | 已建造武器数 |
| `taskState` | TaskState | `IDLE` | 任务状态机 |
| `currentTaskPointIdx` | int | `0` | 当前任务点索引 |
| `taskAcceptRound` | int | `0` | 领取任务回合 |
| `taskTimeoutRounds` | int | `0` | 任务超时回合数 |
| `bestAnswer` | String | `""` | 当前最佳答案 |
| `pendingCmds` | LinkedList\<String\> | `[]` | 沙盒待执行命令 |
| `awaitingCmdResult` | boolean | `false` | 等待沙盒结果 |
| `lastSubmittedCmd` | String | `""` | 上次提交的沙盒命令 |
| `knownMines` | Map\<String,Set\<Pos\>\> | `{}` | 已知矿点 |
| `workerTargetMine` | Map\<Integer,Pos\> | `{}` | 工人→目标矿点 |
| `workerMining` | Map\<Integer,Boolean\> | `{}` | 工人状态 |

- **嵌套枚举 `TaskState`**：`IDLE` → `MOVING_TO_TASK_POINT` → `TASK_ACCEPTED` → `AWAIT_LLM` → `AWAIT_CMD` → `READY_TO_SUBMIT` → `SUBMITTED`

#### POJO 类（仅 public 字段，无方法）

| 类 | 字段 |
|----|------|
| `Zone` | `Pos pos`, `String neutralType` |
| `MapInfo` | `int width`, `int height`, `List<Zone> zones` |
| `Role` | `int id`, `Pos pos`, `String roleType`, `int health`, `int attackPower`, `int attackRange`, `int backPackCapability`, `List<String> backpack`, `int level`, `int cooldown` |
| `PlayerTask` | `String taskType`, `Pos taskPosition`, `int coldDownRounds`, `int scoreReward`, `int goldReward`, `boolean isValid`, `int timeoutRounds` |
| `TeamOur` | `String type`, `String teamId`, `String teamName`, `int goldNum`, `int totalScore`, `List<PlayerTask> playerTasks`, `List<Role> roles` |
| `TeamEnemy` | `List<Role> roles` |
| `Robot` | `List<RobotRole> roles` |
| `RobotRole` | `int id`, `Pos pos`, `String roleType`, `int health`, `String abnormalState`, `String targetTeam` |
| `WorldNews` | `String officialNews`, `String folkLegends` |
| `ErrorInfo` | `int errorCode`, `String description` |
| `ShopItem` | `String name`, `int price` |
| `GameRequest` | 14 个字段，见 §3.1 |
| `GameResponse` | `Map<Integer,RoleCommand> roleCommandMap`, `String prompt`, `String executeCmd` |

> **设计约束**：所有 POJO 字段均为 public 且无 setter，Gson 通过反射直接赋值。如需添加新字段，直接在对应类中添加 public 字段即可，Gson 会自动映射同名的 JSON key。

### 10.3 util 包

#### Constants (final class, private 构造)
全部为 `public static final` 常量。按类别：
- 阵营：`TEAM_CHALLENGER`, `TEAM_DEFENDER`
- 角色类型：`ROLE_STATION`, `ROLE_GATLING`, `ROLE_RAILGUN`, `ROLE_ROCKET`, `ROLE_WALL`, `ROLE_PIONEER`, `ROLE_WORKER`
- 机器人类型：`ROBOT_SMALL`, `ROBOT_MIDDLE`, `ROBOT_LARGE`, `ROBOT_BOSS`
- 矿石：`ORE_STONE`, `ORE_IRON`, `ORE_COPPER`
- 中立元素：`NEUTRAL_VENDOR`, `NEUTRAL_WEAPON_SHOP`
- 动作码：`ACTION_MOVE` ~ `ACTION_COLLECT`（12 个）
- 物品名：升级券 6 个、消耗品 4 个、召唤令 4 个、任务用品 6 个
- 时间参数：`ROUNDS_PER_DAY=130`, `DAY_ROUNDS=70`, `NIGHT_ROUNDS=60`, `MAX_ROUNDS=1300`
- 角色属性：`PIONEER_HP=200`, `WORKER_HP=220`, `PIONEER_BACKPACK=40`, `WORKER_BACKPACK=100`, `MAX_WEAPONS=3`, `RESPAWN_ROUNDS=20`

#### MapUtil (final class, 全 static)
```java
public static int chebyshev(Pos a, Pos b)
public static int chebyshev(Pos a, int bx, int by)
public static boolean isAdjacent(Pos a, Pos b)                    // 距离==1
public static boolean isAdjacentOrSame(Pos a, Pos b)              // 距离<=1
public static Pos stepToward(Pos from, Pos to)                    // 8方向直朝目标一步
public static Pos nextStepToward(Pos from, Pos target, List<Pos> obstacles, int mapWidth, int mapHeight)
public static boolean isValidPos(Pos p, int mapWidth, int mapHeight)
public static boolean isBlocked(Pos p, List<Pos> obstacles)
public static boolean isDayTime(int roundNo)                      // roundNo%130 < 70
public static int getDay(int roundNo)                             // roundNo/130
public static List<Zone> findZones(List<Zone> zones, String neutralType)
public static Role findNearestRole(List<Role> roles, Pos pos)
public static List<Pos> collectObstacles(List<Role> ourRoles, List<Role> enemyRoles, List<RobotRole> robots, List<Zone> zones)
```
- `nextStepToward` 算法：尝试直接朝目标 → 纯水平 → 纯垂直 → 扫描 8 邻域取最近。返回 `from` 本身表示无法移动。

#### RoleUtil (final class, 全 static)
```java
public static List<Role> filterByType(List<Role> roles, String roleType)
public static Role findFirst(List<Role> roles, String roleType)
public static Role findById(List<Role> roles, int id)
public static int countInBackpack(Role role, String itemName)
public static boolean hasInBackpack(Role role, String itemName)
public static boolean isBackpackFull(Role role)
public static int backpackRemaining(Role role)
public static boolean isAlive(List<Role> roles, int roleId)
public static List<Pos> getStationTiles(Pos stationPos)           // 基地2×2四格
public static boolean isAdjacentTo(Role role, Pos target)         // 距离<=1
```

### 10.4 llm 包

#### LlmClient (全 static)
```java
public static boolean canCallLlm(GameContext ctx, boolean inTask)  // 任务期间恒 true；否则检查 <3
public static String buildTaskPrompt(String taskDescription, String lastLlmResp)    // 自进化任务 prompt
public static String buildTreasurePrompt(WorldNews news, String lastLlmResp)        // 宝藏推理 prompt，要求 JSON 输出
public static String extractAnswer(String llmResp)                 // 去除 markdown 代码块，返回纯文本
public static String extractCommand(String llmResp)                // 提取 ```bash/```python 代码块内容
```

### 10.5 sandbox 包

#### SandboxExecutor (全 static)
```java
public static CmdResult parseResult(String lastCmdResult)          // 解析 [exitCode:N]/[TIMEOUT]/[JUDGER_ERROR]/[TRUNCATED]
public static String buildPythonCmd(String script)                 // 包裹为 python3 -c "..."
public static String buildShellCmd(String cmd)                     // 原样返回
```

#### SandboxExecutor.CmdResult (嵌套类)
- public 字段：`boolean empty`, `boolean timeout`, `boolean judgerError`, `boolean truncated`, `int exitCode`, `String output`, `String raw`
- public 方法：`boolean isSuccess()` — 非空且非超时且非判题器错误且 exitCode==0

### 10.6 strategy 包

#### DecisionEngine
```java
public GameResponse decide(GameRequest request)                    // 唯一入口：返回完整响应
private void initContext(GameRequest request)                      // 首回合初始化 teamType/stationPos
private int countWeapons(GameRequest request)                      // 统计 gatling/railgun/rocket 数量
```
- 持有字段：`GameContext ctx`, `DayStrategy dayStrategy`, `NightStrategy nightStrategy`

#### DayStrategy
```java
public void execute(GameResponse response, GameRequest request, GameContext ctx, Set<Integer> occupiedRoles)
```
- 持有字段：`WorkerStrategy workerStrategy`, `PioneerStrategy pioneerStrategy`
- 调用顺序：先 WorkerStrategy（工人），后 PioneerStrategy（开拓者）。

#### NightStrategy
```java
public void execute(GameResponse response, GameRequest request, GameContext ctx, Set<Integer> occupiedRoles)
```
- 持有字段：`CombatStrategy combatStrategy`, `WorkerStrategy workerStrategy`, `PioneerStrategy pioneerStrategy`
- 调用顺序：先 CombatStrategy（攻击优先），后工人/开拓者移动。

#### WorkerStrategy
```java
public void execute(List<Role> workers, GameResponse response, GameRequest request, GameContext ctx, Set<Integer> occupiedRoles, boolean isDay)
```
- private 方法链（决策优先级从高到低）：

| 方法 | 职责 |
|------|------|
| `decideWorkerAction(...)` | 总调度，按优先级调用下列方法 |
| `decideNightAction(...)` | 夜晚：移动到最近武器旁；血量低用药品 |
| `sellMostValuable(...)` | 在小贩旁卖最值钱矿石（按 vendorShopList 价格排序） |
| `tryBuildNearBase(...)` | 在基地附近 5 个候选位中找空位建造围墙 |
| `tryUpgrade(...)` | 有升级券→移动到建筑旁使用；无券→移动到武器商店购买 |
| `selectBestMine(...)` | 选择距离工人最近的矿点 |
| `findZone(...)` | 从 mapInfo.zones 查找指定 neutralType |
| `getShopPrice(...)` | 从 vendorShopList 查矿石价格 |
| `findWeaponToUpgrade(...)` | 找 level<3 的武器工事 |

#### PioneerStrategy
```java
public void execute(Role pioneer, GameResponse response, GameRequest request, GameContext ctx, Set<Integer> occupiedRoles, boolean isDay)
```
- private 方法：

| 方法 | 职责 |
|------|------|
| `trySummonTreasure(...)` | 背包有任务用品→移动到任务点→summonTreasure |
| `collectTreasureItems(...)` | 扫描背包中 6 种任务用品 |
| `moveToTaskPoint(...)` | 移动到 playerTasks[0] 坐标 |
| `decideNightAction(...)` | 夜晚：移动到最近武器旁 |

#### CombatStrategy
```java
public void execute(GameResponse response, GameRequest request, GameContext ctx, Set<Integer> occupiedRoles)
```
- private 方法：

| 方法 | 职责 |
|------|------|
| `filterTargetRobots(...)` | 筛选 targetTeam 为我方的机器人，无则返回全部 |
| `isWeapon(String)` | 判断 roleType 是否为武器工事 |
| `findController(...)` | 找武器周围一格内的非建筑角色；无则返回最近角色 |
| `selectGatlingTargets(...)` | 等级决定目标数，90°锥形约束，按威胁值排序 |
| `selectRailgunTarget(...)` | 单目标，选攻击距离内威胁最大的 |
| `selectRocketTargets(...)` | 选3×3密集区，等级决定导弹数，落点不重叠 |
| `countRobotsInArea(...)` | 统计中心点周围 8 格内机器人数 |
| `getRobotThreat(...)` | 威胁值 = 基础分(小1/中2/大4/BOSS10) × 当前血量 |
| `angleDiff(...)` | 归一化角度差到 [-π, π] |

#### TaskSolver
```java
public RoleCommand solve(Role pioneer, GameResponse response, GameRequest request, GameContext ctx, List<Pos> obstacles, int mapWidth, int mapHeight)
```
- private 方法（对应状态机各状态）：

| 方法 | 触发状态 | 职责 |
|------|----------|------|
| `startNewTask(...)` | IDLE | 找可用任务点→到达则 acceptTask→TASK_ACCEPTED；否则移动→MOVING_TO_TASK_POINT |
| `continueMovingToTaskPoint(...)` | MOVING_TO_TASK_POINT | 继续移动，到达则 acceptTask |
| `requestLlm(...)` | TASK_ACCEPTED | 构造 prompt，设到 response.prompt，→AWAIT_LLM |
| `checkLlmResponse(...)` | AWAIT_LLM | 解析 llmResp→有命令则提交 executeCmd→AWAIT_CMD；有答案→READY_TO_SUBMIT |
| `checkCmdResult(...)` | AWAIT_CMD | 解析 lastCmdResult→成功则用输出做答案→READY_TO_SUBMIT |
| `submitBestAnswer(...)` | READY_TO_SUBMIT | submitAnswer(bestAnswer)→SUBMITTED |
| `hasTaskError(...)` | — | 检查 errors 中 errorCode==1或2，重置状态机 |

---

## 11 类间依赖关系

### 11.1 依赖图

```
MyHttpHandler
  ├── Gson (外部库)
  ├── GameRequest (model)
  ├── GameResponse (model)
  └── DecisionEngine (strategy)
        ├── GameContext (model)
        ├── DayStrategy (strategy)
        │     ├── WorkerStrategy → RoleCommand, Constants, MapUtil, RoleUtil
        │     └── PioneerStrategy → RoleCommand, Constants, MapUtil, RoleUtil
        │           └── TaskSolver → LlmClient, SandboxExecutor, RoleCommand
        └── NightStrategy (strategy)
              ├── CombatStrategy → RoleCommand, Constants, MapUtil
              ├── WorkerStrategy (同上)
              └── PioneerStrategy (同上)
```

### 11.2 数据流

```
Request JSON
  → Gson → GameRequest (POJO)
    → DecisionEngine.decide()
      → 读写 GameContext (跨回合状态)
      → 调用 strategy 层
        → 读取 GameRequest 字段 (角色/地图/机器人/商店/新闻)
        → 写入 GameResponse.roleCommandMap (Map<角色ID, RoleCommand>)
        → 写入 GameResponse.prompt / executeCmd
    → Gson → Response JSON
```

### 11.3 跨回合状态流

```
回合 N:
  DecisionEngine.decide(request_N)
    → ctx.refreshDay(request_N.roundNo)     // 换日重置 LLM 计数
    → ctx.recordMines(request_N.mapInfo)    // 累积矿点记忆
    → ctx.weaponCount = countWeapons(...)   // 更新武器数
    → 策略层读写 ctx.taskState / bestAnswer / workerTargetMine 等
    → 返回 response_N

回合 N+1:
  DecisionEngine.decide(request_{N+1})
    → 同一个 ctx 实例被复用
    → 策略层根据 ctx.taskState 决定下一步行为
    → 根据 request_{N+1}.llmResp / lastCmdResult 推进状态机
```

> **关键约束**：`DecisionEngine` 在 `MyHttpHandler` 中作为实例字段创建，整个服务生命周期共享一个实例。`GameContext` 状态在多线程环境下存在竞态风险（HttpServer 线程池 10 线程），但判题器为单回合顺序请求，实际无并发。

---

## 12 扩展指南

### 12.1 新增/修改角色行为策略

**场景**：修改工人的采集逻辑或优先级。

**修改文件**：`strategy/WorkerStrategy.java`

**步骤**：
1. 找到 `decideWorkerAction()` 方法。
2. 该方法按 if-else 优先级链判断行为。修改条件或添加新的分支。
3. 每个分支最终返回一个 `RoleCommand` 或 `null`（跳过本回合）。
4. 将返回的 `RoleCommand` 放入 `response.roleCommandMap.put(worker.id, cmd)` 并将 `worker.id` 加入 `occupiedRoles`。

**示例**：让工人优先采集铜矿而非石矿
```java
// 在 selectBestMine 中加入权重
private Pos selectBestMine(Role worker, List<Zone> mines) {
    Zone best = null;
    int bestScore = Integer.MIN_VALUE;
    for (Zone z : mines) {
        int dist = MapUtil.chebyshev(worker.pos, z.pos);
        int weight = z.neutralType.equals("copper") ? 3 :
                     z.neutralType.equals("iron") ? 2 : 1;
        int score = weight * 10 - dist;  // 权重×10 - 距离
        if (score > bestScore) {
            bestScore = score;
            best = z;
        }
    }
    return best != null ? best.pos : null;
}
```

### 12.2 修改武器攻击目标选择

**场景**：让加特林优先攻击 BOSS 而非距离最近的机器人。

**修改文件**：`strategy/CombatStrategy.java` → `selectGatlingTargets()`

**关键逻辑**：
```java
// 当前排序依据 getRobotThreat() = 基础分 × 血量
// 修改 getRobotThreat 的基础分即可调整优先级
case Constants.ROBOT_BOSS: base = 10; break;  // 改大此值可提高 BOSS 优先级
```

### 12.3 修改任务解题流程

**场景**：LLM 返回的答案需要二次处理后再提交。

**修改文件**：`strategy/TaskSolver.java` → `checkLlmResponse()` 或 `submitBestAnswer()`

```java
// 在 submitBestAnswer 中对答案做后处理
private RoleCommand submitBestAnswer(Role pioneer, GameContext ctx) {
    String answer = ctx.bestAnswer.trim();
    // 自定义后处理逻辑
    answer = answer.replaceAll("\\n", " ");
    RoleCommand cmd = RoleCommand.submitAnswer(answer);
    ctx.taskState = GameContext.TaskState.SUBMITTED;
    return cmd;
}
```

### 12.4 添加新的 LLM prompt 模板

**修改文件**：`llm/LlmClient.java`

添加新的 static 方法：
```java
public static String buildCustomPrompt(String context, String question) {
    StringBuilder sb = new StringBuilder();
    sb.append("你的角色：...\n");
    sb.append("上下文：\n").append(context).append("\n");
    sb.append("问题：").append(question).append("\n");
    sb.append("请以JSON格式回答...\n");
    return sb.toString();
}
```
然后在 `TaskSolver` 或 `PioneerStrategy` 中调用，将返回值赋给 `response.prompt`。

### 12.5 适配接口文档版本升级

**场景**：接口文档新增字段（如 Request 新增 `weatherInfo` 字段）。

**步骤**：
1. 在 `model/` 下新建或修改对应 POJO 类，添加 public 字段。
2. 在 `GameRequest` 中添加该字段。
3. 在 `Constants` 中添加相关常量。
4. 在策略层中读取该字段并编写决策逻辑。
5. 更新 `makelist.txt`（若新建了文件）。
6. 重新编译验证。

### 12.6 新增策略类

**场景**：需要独立的"资源管理策略"或"敌方分析策略"。

**步骤**：
1. 在 `strategy/` 包下新建类，构造方法接收所需依赖。
2. 实现 `execute(GameResponse, GameRequest, GameContext, Set<Integer>)` 方法。
3. 在 `DayStrategy` 或 `NightStrategy` 的 `execute()` 中调用新策略。
4. 更新 `makelist.txt`。
5. 编译验证。

### 12.7 调整游戏时间参数

**场景**：规则修改为白天 80 回合、夜晚 50 回合。

**修改文件**：`util/Constants.java`
```java
public static final int DAY_ROUNDS = 80;    // 原 70
public static final int NIGHT_ROUNDS = 50;  // 原 60
// ROUNDS_PER_DAY 保持 130 不变
```
`MapUtil.isDayTime()` 自动使用新常量，无需其他修改。

---

## 13 已知局限与改进方向

### 13.1 路径规划

**当前实现**：`MapUtil.nextStepToward()` 采用贪心单步策略（直接→水平→垂直→对角），不考虑全局最优路径。

**局限**：在复杂障碍物场景下可能陷入局部最优，角色在障碍物前反复尝试。

**改进方向**：实现 BFS/A* 全局路径规划，缓存路径直到目标达成或障碍物变化。

### 13.2 武器建造位置

**当前实现**：`WorkerStrategy.tryBuildNearBase()` 在基地附近 5 个硬编码候选位中选空位。

**局限**：不考虑敌方进攻路线，可能建造在无效位置。

**改进方向**：分析敌方机器人出现位置和移动路径，在进攻路线上建造。

### 13.3 宝藏推理

**当前实现**：`PioneerStrategy.trySummonTreasure()` 简单地将任务点 1 作为祭坛位置，背包中有任务用品就尝试召唤。

**局限**：未解析 `worldNews.folkLegends` 内容推断真实祭坛位置和所需物品组合。

**改进方向**：用 `LlmClient.buildTreasurePrompt()` 解析民间传闻，提取 JSON 格式的位置/物品/时间，按推断结果精确召唤。

### 13.4 矿石价格波动

**当前实现**：`sellMostValuable()` 按 `vendorShopList` 当前价格排序卖出。

**局限**：未解析 `worldNews.officialNews` 预测价格变化，可能在低价时卖出。

**改进方向**：解析官方消息中的供需事件，预测未来价格走势，在价格峰值时卖出。

### 13.5 敌方信息利用

**当前实现**：仅读取 `teamEnemy.roles` 作为障碍物，未分析敌方策略。

**改进方向**：跟踪敌方建筑升级进度、角色移动模式，预测敌方战术并调整己方策略。

### 13.6 并发安全

**当前实现**：`DecisionEngine` 为单实例，`GameContext` 非线程安全。

**当前可行原因**：判题器单回合顺序请求，无实际并发。

**改进方向**：若需支持并发调试或多对局隔离，改为每请求创建独立 Context 或加锁。

### 13.7 围墙建造策略

**当前实现**：仅在 `weaponCount < 3` 且有石头时建造围墙，位置固定。

**局限**：不评估围墙的防御价值，可能浪费石头。

**改进方向**：根据机器人进攻方向计算围墙防御收益，优先在关键路径建造。

---

## 14 调试与排查指南

### 14.1 本地调试

```bash
# 1. 编译
cd D:/Codes/CoreGeek/src
javac -Xlint:unchecked -Xlint:-options -source 1.8 -target 1.8 \
  -d ../bin -encoding UTF-8 -Djava.ext.dirs=../lib @../../makelist.txt

# 2. 启动服务
cd D:/Codes/CoreGeek
java -cp "bin;lib/*" com.huawei.Main 8080

# 3. 发送测试请求
curl -s -X POST -H "Content-Type: application/json" \
  -d @D:/Codes/doc/request.txt http://localhost:8080/ | python -m json.tool
```

### 14.2 日志

当前无日志框架集成。如需添加调试输出：
- 在 `MyHttpHandler.handle()` 中打印 Request/Response JSON。
- 在策略层方法入口打印决策上下文。
- 使用 `System.err.println()` 避免干扰 HTTP 响应。

### 14.3 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| 返回 `{}` | Request JSON 解析失败 | 检查 `MyHttpHandler` 异常堆栈 |
| 角色无指令 | 被 `occupiedRoles` 标记或策略返回 null | 检查策略优先级链是否有分支遗漏 |
| 指令判非法 | 字段缺失或动作条件不满足 | 对照 §9.1 检查指令完整性 |
| LLM 调用报错 | 超过每日 3 次限额 | 检查 `ctx.llmCallCountToday` 和 `phaseTask` 是否非空 |
| 任务超时 | `taskAcceptRound + timeoutRounds` 超限 | 检查 `TaskSolver` 状态机是否推进 |
| 编译找不到类 | `makelist.txt` 未包含新文件 | 确认文件路径相对于 `src/` 目录 |
| `Could not create JVM` | JDK 9+ 不支持 `-Djava.ext.dirs` | 改用 `-cp "bin;lib/*"` |

### 14.4 修改后验证清单

每次修改后按以下步骤验证：
1. **编译通过**：`javac` 无 error（warning 可忽略）。
2. **服务启动**：`java -cp` 正常启动无异常。
3. **夜晚场景**：用 `request.txt`（roundNo=85）测试，验证 attack 指令生成。
4. **白天场景**：修改 roundNo=10 测试，验证 collect/move 指令生成。
5. **JSON 格式**：返回体可被 `python -m json.tool` 正常解析。
6. **角色覆盖**：确认 `roleCommandMap` 中每个角色 ID 只出现一次。

---

## 15 文件清单与变更追踪

### 15.1 源文件清单（30 个 Java 文件）

| 包 | 文件 | 行数 | 职责 |
|----|------|------|------|
| 根 | `Main.java` | ~28 | 启动 HttpServer |
| 根 | `MyHttpHandler.java` | ~52 | HTTP 请求/响应处理 |
| model | `Pos.java` | ~50 | 坐标 + 切比雪夫距离 |
| model | `Zone.java` | ~8 | 中立元素 |
| model | `MapInfo.java` | ~12 | 地图信息 |
| model | `Role.java` | ~18 | 单位属性 |
| model | `PlayerTask.java` | ~14 | 任务点信息 |
| model | `TeamOur.java` | ~16 | 我方队伍 |
| model | `TeamEnemy.java` | ~8 | 敌方队伍 |
| model | `Robot.java` | ~8 | 机器人集合 |
| model | `RobotRole.java` | ~12 | 单个机器人 |
| model | `WorldNews.java` | ~8 | 世界新闻 |
| model | `ErrorInfo.java` | ~8 | 错误信息 |
| model | `ShopItem.java` | ~8 | 商店物品 |
| model | `GameRequest.java` | ~22 | 顶层 Request |
| model | `GameResponse.java` | ~14 | 顶层 Response |
| model | `RoleCommand.java` | ~115 | 角色指令 + 12 种工厂方法 |
| model | `GameContext.java` | ~105 | 跨回合状态 + TaskState 枚举 |
| strategy | `DecisionEngine.java` | ~75 | 总调度入口 |
| strategy | `DayStrategy.java` | ~25 | 白天策略调度 |
| strategy | `NightStrategy.java` | ~30 | 夜晚策略调度 |
| strategy | `WorkerStrategy.java` | ~240 | 工人行为逻辑 |
| strategy | `PioneerStrategy.java` | ~140 | 开拓者行为逻辑 |
| strategy | `CombatStrategy.java` | ~235 | 武器操控与目标选择 |
| strategy | `TaskSolver.java` | ~180 | 自进化任务状态机 |
| llm | `LlmClient.java` | ~80 | LLM prompt 构造与解析 |
| sandbox | `SandboxExecutor.java` | ~75 | 沙盒命令与结果解析 |
| util | `Constants.java` | ~110 | 常量定义 |
| util | `MapUtil.java` | ~150 | 地图/距离/寻路工具 |
| util | `RoleUtil.java` | ~95 | 角色查找/背包工具 |

### 15.2 配置文件

| 文件 | 位置 | 说明 |
|------|------|------|
| `makelist.txt` | `D:/Codes/` | 编译源文件列表，每行一个路径（相对 `src/`）。新增 Java 文件必须在此添加 |
| `log4j.properties` | `CoreGeek/src/main/resources/` | 日志配置（当前未使用） |
| `gson-2.8.2.jar` | `CoreGeek/lib/` | JSON 序列化库 |

### 15.3 变更追踪

修改代码时建议在此记录：

| 日期 | 修改文件 | 修改内容 | 修改人 |
|------|----------|----------|--------|
| 2026-09-20 | 全部 | 初始实现 | - |

---

## 16 关键约束速查

| 约束 | 值 | 影响 |
|------|----|------|
| 响应超时 | 5 秒 | 决策逻辑必须秒级完成 |
| 连接超时 | 10 秒 | 服务启动须快速 |
| 异常上限 | 5 次 | 累计 5 次异常停止调度 |
| 沙盒超时 | 15 秒 | executeCmd 须在 15 秒内完成 |
| LLM 日限额 | 3 次/游戏日 | 任务期间不限 |
| 武器上限 | 3 座 | 新建覆盖旧武器 |
| 每角色每回合 | 1 条指令 | roleCommandMap 中 ID 不可重复 |
| 地图尺寸 | 41×32 | 坐标范围 [0,40]×[0,31] |
| 最大回合 | 1300 | 10 天 × 130 回合 |
| 复活等待 | 20 回合 | 阵亡后第二天白天 +20 回合 |
